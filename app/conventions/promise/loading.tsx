import { Loading } from "@/components/Loading";
type Props = {};

const LoadingPage = (props: Props) => {
  return (
    <div>
      <Loading></Loading>
    </div>
  );
};

export default LoadingPage;
